#그래프 한글 깨짐 방지
from matplotlib import font_manager, rc
font_path = 'c:/Windows/Fonts/malgun.ttf'  # font의 저장된 경로에서 불러오기
font_name = font_manager.FontProperties(fname=font_path).get_name()
rc('font', family=font_name)
